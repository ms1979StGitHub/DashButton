add files to repo
=================

1) git add -A
2) git commit -a -m "add x files to repo"
3) git push -u origin master

commit changes
git commit -a -m "MESSAGE"

push changes to repo
git push -u origin master